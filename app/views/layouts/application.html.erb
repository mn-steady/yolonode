<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Yolonode" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <!-- Favicon for browsers -->
    <link rel="icon" href="<%= asset_path('favicon.ico') %>" type="image/x-icon">
    <link rel="icon" href="/icon.png" type="image/png" sizes="16x16">
    <link rel="icon" href="/icon.png" type="image/png" sizes="32x32">
    <!-- Apple touch icon for iOS -->
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180">
    <!-- Icon for Android Chrome -->
    <link rel="icon" type="image/png" href="/android-chrome-192x192.png" sizes="192x192">
    <link rel="icon" type="image/png" href="/android-chrome-512x512.png" sizes="512x512">
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- JavaScript function for "Report A Bug" button -->
    <script>
      function openMailtoWithTimestamp() {
        const currentDate = new Date();
        const timestamp = currentDate.toISOString().split('T')[0]; // YYYY-MM-DD format
        const mailtoLink = `mailto:steady@yolonode.com?subject=Bug%20Report:%20${timestamp}`;
        window.location.href = mailtoLink;
      }
    </script>
  </head>

  <body>
    <!-- Banner Section -->
    <div class="banner">
      Producing blocks on Secret Network since 7/10/2024!
    </div>

    <div class="image-section">
        <div class="button-container">
            <a href="https://www.mintscan.io/secret/validators/secretvaloper1pkzmfk34qg46p4hen0dnlkn05rzje65xk4tzjc" class="yellow-button" target="_blank">Validator Info</a>
            <a href="https://dash.scrt.network/staking" class="yellow-button" target="_blank">Stake With Us!</a>
        </div>
    </div>

    <%= yield %>
  </body>
</html>
